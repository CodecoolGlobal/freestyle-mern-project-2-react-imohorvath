{
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^6.10.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "scripts": {
    "dev": "nodemon server.js",
    "populate": "node ./populate/populate.js",
    "update": "node ./populate/update.js",
    "prod": "node server.js",
    "populate-start": "npm run populate && npm run prod"
  }
}
